package ao.gabrielvieira.fkiosklite.kiosk

import io.flutter.embedding.android.FlutterActivity

class KioskActivity : FlutterActivity() {
    // lockTaskMode="if_whitelisted" is declared in AndroidManifest.xml.
    // This activity subclass exists so that the manifest can reference it.
}
