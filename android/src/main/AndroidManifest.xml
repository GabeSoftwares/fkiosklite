<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ao.gabrielvieira.fkiosk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application>
        <!-- Device Admin Receiver -->
        <receiver
            android:name=".dpc.AdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Kiosk Activity with Lock Task support -->
        <activity
            android:name=".kiosk.KioskActivity"
            android:lockTaskMode="if_whitelisted"
            android:launchMode="singleTask"
            android:exported="true" />

        <!-- Install status BroadcastReceiver -->
        <receiver
            android:name=".update.UpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ao.gabrielvieira.fkiosk.ACTION_INSTALL_STATUS" />
                <action android:name="ao.gabrielvieira.fkiosk.ACTION_UNINSTALL_STATUS" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
